import { ApplicationDescription } from './application-description';

describe('ApplicationDescription', () => {
  it('should create an instance', () => {
    expect(new ApplicationDescription()).toBeTruthy();
  });
});
