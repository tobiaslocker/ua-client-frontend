import { EndpointDescription } from './endpoint-description';

describe('EndpointDescription', () => {
  it('should create an instance', () => {
    expect(new EndpointDescription()).toBeTruthy();
  });
});
